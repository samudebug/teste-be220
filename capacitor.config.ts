import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.samuel.usinaplay',
  appName: 'usinaplay',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
